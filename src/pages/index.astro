---
import BaseLayout from "../layouts/BaseLayout.astro";
import ProfileLink from "../components/ProfileLink.astro";
import {
  experience,
  profile,
  publications,
  researchHighlights,
} from "../data/site";

const featuredPublications = publications.filter((publication) => publication.featured);
const selectedExperience = experience.slice(0, 3);
---

<BaseLayout>
  <section class="container hero">
    <div class="hero-copy">
      <div class="hero-identity">
        <img class="portrait" src={profile.image} alt={profile.imageAlt} width="500" height="500" loading="eager" />
        <div>
          <h1>{profile.name}</h1>
          <p class="title-lines">
            {profile.titleLines.map((line) => <span>{line}</span>)}
          </p>
        </div>
      </div>
      <p class="lede">{profile.summary}</p>
      <ul class="tag-list" aria-label="Research interests">
        {profile.interests.map((interest) => <li class="tag">{interest}</li>)}
      </ul>
      <ul class="contact-list" aria-label="Contact and profile links">
        {
          profile.links.slice(0, 3).map((link) => (
            <li>
              <ProfileLink label={link.label} href={link.href} icon={link.icon} />
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <section class="section soft">
    <div class="container">
      <div class="section-header">
        <div>
          <p class="eyebrow">Research</p>
          <h2>LLM-based motivational interviewing systems</h2>
        </div>
        <a class="button" href="/research/">Read more</a>
      </div>
      <div class="grid three">
        {
          researchHighlights.map((highlight) => (
            <article class="card">
              <h3>{highlight.title}</h3>
              <p class="text-muted">{highlight.body}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="section-header">
        <div>
          <p class="eyebrow">Selected publications</p>
          <h2>Recent research output</h2>
        </div>
        <a class="button" href="/publications/">All publications</a>
      </div>
      <div class="grid three">
        {
          featuredPublications.map((publication) => (
            <article class="card publication-card">
              <div>
                <p class="card-meta">{publication.venue} · {publication.year}</p>
                <h3>
                  <a class="publication-title" href={publication.href} rel="noreferrer" target="_blank">
                    {publication.title}
                  </a>
                </h3>
              </div>
              <p class="publication-authors">{publication.authors}</p>
              <div class="publication-footer">
                {publication.tags.map((tag) => <span class="mini-tag">{tag}</span>)}
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section soft">
    <div class="container">
      <div class="section-header">
        <div>
          <p class="eyebrow">Experience</p>
          <h2>Research, clinical, and engineering background</h2>
        </div>
        <a class="button" href="/cv/">Full CV</a>
      </div>
      <div class="timeline">
        {
          selectedExperience.map((item) => (
            <article class="timeline-item">
              <div class="timeline-date">{item.date}</div>
              <div>
                <h3>{item.role}, {item.organization}</h3>
                <p class="card-meta">{item.location} · {item.type}</p>
                <p class="text-muted">{item.bullets[0]}</p>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>
</BaseLayout>
